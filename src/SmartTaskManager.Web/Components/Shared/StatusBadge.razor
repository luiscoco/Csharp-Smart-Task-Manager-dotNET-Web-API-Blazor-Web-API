<span class="@CssClass">@Text</span>

@code {
    [Parameter, EditorRequired]
    public string Text { get; set; } = string.Empty;

    [Parameter]
    public string Tone { get; set; } = "neutral";

    private string CssClass => $"badge badge--{Tone}";
}
